import styled from 'styled-components';

const Background = styled.div`
  background: #113264;
  color: #fff;
  min-height: 100vh;
  overflow: hidden;
  position: relative;
`;

export default Background;
